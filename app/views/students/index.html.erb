<h1>👤 These are some cool students: 👤</h1>

<ul>
  <% @students.each do |student| %>
    <li> <%= link_to student.name, student %> </li>
  <% end %>
</ul>

<%= link_to '<button type="button">Create Student</button>'.html_safe, new_student_path %>
